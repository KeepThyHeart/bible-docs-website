# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Scratch data directory the screenshot script builds for its server
/.screenshot-data

# Captured screenshots. Reproducible from `npm run screenshots`, and several
# hundred KB each, so they are built at deploy time rather than committed.
/static/img/web
/static/img/desktop

# Scratch output from probing the desktop app during capture development.
/scratch-shots

# Local-only files: the publish script (server login and paths) and the like.
*.private.*
*.ignore.*
scratchpad
