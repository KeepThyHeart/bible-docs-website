import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

// Each docs plugin instance (desktop, web) gets its own sidebar file. This
// file exists only as a placeholder so editor tooling that opens the website
// root can still find a sidebars module.
const sidebars: SidebarsConfig = {};

export default sidebars;
